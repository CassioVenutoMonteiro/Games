using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovPontos : MonoBehaviour
{
    //public Transform[] Posicoes;
    //Transform PosAtual;
    //int pos;
    //float velocidade;
    // Start is called before the first frame update
    void Start()
    {
        //pos = 0;
        //PosAtual = Posicoes[pos];
        //velocidade = 5;
    }

    // Update is called once per frame
    void Update()
    {
    //    transform.position = Vector2.MoveTowards(transform.position,PosAtual.position,velocidade * Time.deltaTime);

    //    if(transform.position.x == PosAtual.position.x && transform.position.y == PosAtual.position.y)
    //    {
    //        pos++;
    //        if(pos >= Posicoes.Length)
    //        {
    //            pos = 0;
    //        }
    //        PosAtual = Posicoes[pos];

    //  }
    }
}
